"use client";

/**
 * Social login placeholder — Google OAuth đã bị tắt.
 * Người dùng chỉ đăng nhập bằng email + mật khẩu.
 * Component này giữ lại để không break import trong login/register pages.
 */
export default function SocialLoginButtons() {
  return null;
}
